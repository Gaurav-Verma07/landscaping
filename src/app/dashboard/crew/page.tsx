import { ModulePlaceholder } from "@/components/dashboard/ui/module-placeholder"

export default function CrewPage() {
  return (
    <ModulePlaceholder
      title="Labor & Crew Management"
      description="Time tracking: clock in/out, project selection, GPS verification, supervisor override. Employee fields: skill level, certifications, role, availability. Crew scheduling by skill, avoid double-booking, equipment dependency check."
    />
  )
}
