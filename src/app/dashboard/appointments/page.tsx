import { ModulePlaceholder } from "@/components/dashboard/ui/module-placeholder"

export default function AppointmentsPage() {
  return (
    <ModulePlaceholder
      title="Appointments & Calendar"
      description="Appointment booking, AI scheduling assistant, calendar events, assign to users, conflict detection. Events: customer, project, address, date/time, assigned users, equipment, notes."
    />
  )
}
